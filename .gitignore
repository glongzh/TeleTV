*.iml
.gradle/
/local.properties
.idea/
.kotlin/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
/app/build/
# TDLib native libraries are large binaries fetched separately — do not commit.
# Run `bash app/src/main/jniLibs/fetch.sh` after cloning.
/app/src/main/jniLibs/**/*.so
# Release signing keystore — never commit.
/keystore/
# Per-machine Claude Code settings.
.claude/settings.local.json
